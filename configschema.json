{
    "$id": "http://example.com/example.json",
    "$schema": "http://json-schema.org/draft-07/schema",
    "default": {
        "parentURL": "",
        "websocketAddress": "localhost",
        "websocketPassword": "password",
        "intermissionScene": "Intermission"
    },
    "description": "The root schema comprises the entire JSON document.",
    "examples": [
        {
            "parentURL": "",
            "websocketAddress": "localhost",
            "websocketPassword": "password",
            "intermissionScene": "Intermission"
        }
    ],
    "required": [
        "parentURL",
        "websocketAddress",
        "websocketPassword",
        "intermissionScene"
    ],
    "title": "The root schema",
    "type": "object",
    "properties": {
        "parentURL": {
            "$id": "#/properties/parentURL",
            "type": "string",
            "title": "The parentURL schema",
            "description": "An explanation about the purpose of this instance.",
            "default": "",
            "examples": [
                ""
            ]
        },
        "websocketAddress": {
            "$id": "#/properties/websocketAddress",
            "type": "string",
            "title": "The websocketAddress schema",
            "description": "An explanation about the purpose of this instance.",
            "default": "",
            "examples": [
                "localhost"
            ]
        },
        "websocketPassword": {
            "$id": "#/properties/websocketPassword",
            "type": "string",
            "title": "The websocketPassword schema",
            "description": "An explanation about the purpose of this instance.",
            "default": "",
            "examples": [
                "password"
            ]
        },
        "intermissionScene": {
            "$id": "#/properties/intermissionScene",
            "type": "string",
            "title": "The intermissionScene schema",
            "description": "An explanation about the purpose of this instance.",
            "default": "",
            "examples": [
                "Intermission"
            ]
        }
    },
    "additionalProperties": true
}